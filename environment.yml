name: hackathon-onera
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - gnuradio>=3.10
  - numpy>=1.24
  - scikit-learn>=1.3
  # PyTorch — conda-forge provides CPU builds; use pytorch channel for GPU
  - pytorch::pytorch>=2.1.0
  - pip:
    - pyrtlsdr>=0.3.0
