# PyTorch (CPU only — replace with torch[cuda] or cu118/cu121 wheel for GPU)
torch>=2.1.0

# Scientific computing
numpy>=1.24.0
scikit-learn>=1.3.0

# RTL-SDR Python bindings
pyrtlsdr>=0.3.0

# GNU Radio Python bindings are installed separately via conda or the system
# package manager (apt, dnf, …). Do NOT install gnuradio via pip.
# See environment.yml for a full conda-based setup.
